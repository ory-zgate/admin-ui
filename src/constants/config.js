export const LOGIN_URL = window.LOGIN_URL || "http://127.0.0.1:3000/auth/login";
export const LOGOUT_URL = window.LOGOUT_URL || "http://127.0.0.1:3000/auth/logout";

export default {
  url: {
    login: LOGIN_URL,
    logout: LOGOUT_URL
  }
}